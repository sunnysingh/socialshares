{
  "private": true,
  "scripts": {
    "lerna": "lerna",
    "list": "lerna list",
    "link": "lerna link",
    "bootstrap": "lerna clean && lerna bootstrap --no-ci --force-local",
    "build": "lerna run build",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
