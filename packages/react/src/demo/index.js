import React from 'react';
import ReactDOM from 'react-dom';
import { ShareButtons } from '../socialshares';

const mountElement = document.getElementById('shareButtons');

ReactDOM.render(<ShareButtons />, mountElement);
